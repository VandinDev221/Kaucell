version: "3.9"

services:
  app:
    build: .
    container_name: kaucell-app
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
    environment:
      - ADMIN_USER=admin
      - ADMIN_PASS=1234
    restart: unless-stopped

